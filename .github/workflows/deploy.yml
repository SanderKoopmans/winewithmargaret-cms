name: 🚀 Deploy
on:
  push:
    branches:
      - master
  pull_request: {}

permissions:
  actions: write
  contents: read

defaults:
  run:
    shell: bash

jobs:
  build:
    name: Build Image
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Configure AWS Credentials
      uses: aws-actions/configure-aws-credentials@v1
      with:
        aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
        aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
        aws-region: eu-west-1
      
    - name: Login to ECR
      id: login-ecr
      uses: aws-actions/amazon-ecr-login@v1

    - name: Build, tag, and push image to Amazon ECR
      env:
        ECR_REGISTRY: ${{ steps.login-ecr.outputs.registry }}
        ECR_REPOSITORY: wwm-strapi-api
        IMAGE_TAG: 1
      run: |
        docker build -t $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG -t $ECR_REGISTRY/$ECR_REPOSITORY:latest -f Dockerfile.production .
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:$IMAGE_TAG
        docker push $ECR_REGISTRY/$ECR_REPOSITORY:latest

    # - name: Create and update version on ELB
    #   uses: ItsKarma/aws-cli@v1.70.0
    #   env:
    #     AWS_ACCESS_KEY_ID: ${{ secrets.AWS_ACCESS_KEY_ID }}
    #     AWS_SECRET_ACCESS_KEY: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
    #     AWS_DEFAULT_REGION: "eu-west-1"
    #     IMAGE_TAG: 1
    #   with:
    #     args: |
    #       elasticbeanstalk create-application-version --application-name wwm-strapi-cms --version-label $IMAGE_TAG --source-bundle S3Bucket=wwm-strapi-dockerrun,S3Key=Dockerrun.aws.json
    #       elasticbeanstalk update-environment --application-name wwm-strapi-cms --environment-name wwm-strapi-env --version-label=$IMAGE_TAG
    - name: Install AWS CLI and run a command
        run: |
          echo "----Get aws cli----"
          curl "https://awscli.amazonaws.com/awscli-exe-linux-x86_64.zip" -o "awscliv2.zip"
          echo "----Unzip----"
          unzip awscliv2.zip
          echo "----Install----"
          sudo ./aws/install --update 
          echo "----Check aws----"
          aws --version
          echo "----Configure----"
          aws configure set aws_access_key_id ${{ secrets.AWS_ACCESS_KEY_ID }}
          aws configure set aws_secret_access_key ${{ secrets.AWS_SECRET_ACCESS_KEY }}
          aws configure set default.region ${{ secrets.AWS_REGION }}
          aws elasticbeanstalk create-application-version --application-name wwm-strapi-cms --version-label $IMAGE_TAG --source-bundle S3Bucket=wwm-strapi-dockerrun,S3Key=Dockerrun.aws.json
          aws elasticbeanstalk update-environment --application-name wwm-strapi-cms --environment-name wwm-strapi-env --version-label=$IMAGE_TAG